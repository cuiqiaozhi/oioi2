import { Context } from "../context";
import { NSession } from '../bot';
import { Command } from "@lc-cn/command";
export declare function enableHelp<A extends any[], O extends {}>(cmd: Command<A, O>): Command<A, { [P in "help" | keyof O]?: (P extends keyof O ? O[P] : unknown) & (P extends "help" ? boolean : unknown); }>;
export interface HelpConfig extends Command.Config {
    shortcut?: boolean;
    options?: boolean;
}
export declare function getCommandNames(session: NSession<'message'>): string[];
export declare function install(ctx: Context): void;
