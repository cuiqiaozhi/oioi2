"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@lc-cn/command");
const utils_1 = require("@oitq/utils");
__exportStar(require("@lc-cn/command"), exports);
command_1.Command.prototype.shortcut = function (name, config = {}) {
    config.name = name;
    config.command = this;
    config.authority || (config.authority = this.config.authority);
    this.context.app._shortcuts.push(config);
    this.context.state.disposes.push(() => (0, utils_1.remove)(this.context.app._shortcuts, config));
    return this;
};
command_1.Command.prototype.match = function (session) {
    return this.context.match(session);
};
const oldSubCommand = command_1.Command.prototype.subcommand;
command_1.Command.prototype.subcommand = function (def, ...args) {
    const command = oldSubCommand.bind(this)(def, ...args);
    command.context = this.context;
    this.context.app._commands.set(command.name, command);
    this.context.app._commandList.push(command);
    this.context.state.disposes.push(() => (0, utils_1.remove)(this.context.app._commandList, command), () => {
        this.context.app._commands.delete(command.name);
        return true;
    });
    this.context.emit('command.add', command);
    return command;
};
